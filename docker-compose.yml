services:
  postgres:
    image: postgres:17
    container_name: moldova-price-history-postgres
    environment:
      POSTGRES_DB: pricehistory
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
